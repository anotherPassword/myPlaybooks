---
- name: Check winrm
  hosts: all
    
  tasks:
  
  - name: Check winrm is Installed
    win_service:
     name: winRM
    register: winrmstatus
  - debug:
      msg: "{{ winrmstatus.name }}" 


      ###this is a change
  

### this is another changed_when